{
  "name": "vetta-driver-intelligence",
  "private": true,
  "version": "4.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm run test && node scripts/check-structure.mjs"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
