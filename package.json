{
  "name": "vetta-driver-intelligence",
  "private": true,
  "version": "3.5.1",
  "type": "module",
  "scripts": {
    "test": "npm run test:platform && npm run test:pwa-gate",
    "test:platform": "node --test tests/platform.test.mjs",
    "test:pwa-gate": "node --test tests/pwa-install-gate.test.mjs",
    "test:e2e": "playwright test",
    "test:pages-e2e": "playwright test --config=playwright.pages.config.js",
    "prepare:pages": "node scripts/prepare-pages.mjs",
    "check:platform": "node --check src/platform/manifest.js && node --check src/platform/event-bus.js && node --check src/platform/feature-flags.js && node --check src/platform/module-registry.js && node --check src/platform/diagnostics.js && node --check src/platform/index.js && node --check src/app/platform-runtime.js && node --check src/modules/platform-demo/index.js && npm run test:platform",
    "check:pwa-gate": "node --check src/pwa/install-gate-core.js && node --check pwa-install-gate.js && node --check scripts/prepare-pages.mjs && npm run test:pwa-gate",
    "check:production": "node --check app.js && node --check sw.js && npm run check:platform && node tests/verify-production.mjs && playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.2"
  }
}
